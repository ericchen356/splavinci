node_modules
.next
out
*.tsbuildinfo
next-env.d.ts
.DS_Store
.env
.env.*
!.env.example

# Claude Code: agent definitions are shared project config and ARE committed.
# Personal, machine-local settings are not.
.claude/settings.local.json

# large splat captures - keep out of git
*.spz
*.ply

# derived from a local capture that is not in the repo
public/hobbiton/
public/derived/

public/generated/

# derived from a source video by scripts/video-frames.swift
samples/*/frames/
